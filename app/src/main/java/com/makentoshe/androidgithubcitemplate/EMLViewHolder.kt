package com.makentoshe.androidgithubcitemplate

import android.view.View
import androidx.recyclerview.widget.RecyclerView
import kotlinx.android.synthetic.main.items_main.view.*

class EMLViewHolder {

}
/*
    private val itemClickListener: ElementMainList.onItemClickListener): RecyclerView.ViewHolder(itemView){ //problem with RecyclerView.ViewHolder(itemView
        //toDo
        fun bind(item: ElementMainList){
            with(itemView){
                item.run {
                    nameTextView.text = name
                    descriptionTextView.text = description

                    Glide.with(context).load(avatarUrl).into(photoImageView)

                    deleteItemImageView.setOnClickListener{
                        ItemClickListener.OnItemClicked(id = id)
                    }
                }
            }
        }
    }
}

*/

//EML = "ElementMainList"